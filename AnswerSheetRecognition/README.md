# OMR Research and Development

Given a list of images, tries to recognize the answer sheet.
Runs in batch, so that the overall score can be tracked for each change (algorithm or data set).

**Requirements**

- Python
- OpenCV 
- Numpy
